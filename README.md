# Face Sensor

### Using the DSCframework
* Contains a nodejs sensor that listens to a camera broadcast and detects faces
* Contains a python sensor that listens to the nodejs sensor and recognizes the detected faces
